import React from 'react'
import {View, StyleSheet} from 'react-native';

const Spacer = ({children}) => {
return <View style={styles.spa}>{children}</View>
};

const styles = StyleSheet.create({})

export default Spacer;